[package]
name = "plato-distill"
version = "0.1.0"
edition = "2021"
description = "Progressive knowledge distillation tracking for PLATO rooms"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
serde_json = "1.0.150"
